// This file is no longer used, as we are now using a dynamic detail page
// at /reports/detail. This file can be deleted.
// We are keeping it here for now to avoid breaking any existing links,
// but it should be removed in a future cleanup.
export default function DeprecatedReportDetailPage() {
  return (
    <div className="flex items-center justify-center h-full">
      <p>This page is no longer in use.</p>
    </div>
  );
}
